{
    "name": "natours",
    "version": "1.0.0",
    "description": "",
    "main": "index.js",
    "scripts": {
        "dev": "concurrently \"live-server --open=starter \" \"node-sass ./starter/sass/main.scss ./starter/css/style.css -w\"",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "author": "",
    "license": "ISC",
    "devDependencies": {
        "concurrently": "^4.1.0",
        "node-sass": "^4.10.0"
    },
    "dependencies": {
        "live-server": "^1.2.0"
    }
}
